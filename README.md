# LaunchOS
LaunchOS — a local-first SaaS MVP that turns customer signals into positioning, launch assets, and A/B experiments with conversion tracking. Built with Next.js (App Router), TypeScript, Tailwind, Prisma + SQLite. Mock AI mode included (no external services required).
